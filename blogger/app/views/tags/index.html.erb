<h1>All Tags</h1>

<ul id="tag">
  <% @tag.each do |tag| %>
    <li>
      <%= link_to tag, tag_path(tag), class: 'tag_title' %>

    </li>
  <% end %>
</ul>

<%= link_to "<< Back to Articles List", articles_path %>
